@extends('layouts.app')
@section('content')<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div class="eyebrow">Inventory</div><h1 class="page-title h2 mb-1">Semua aset</h1><p class="text-secondary mb-0">Kelola perangkat, penempatan, dan status lifecycle.</p></div><a href="{{ route('assets.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-2"></i>Tambah aset</a></div>
<div class="panel mb-3"><form class="p-3 row g-2" method="GET"><div class="col-lg-5"><div class="input-group"><span class="input-group-text bg-white"><i class="bi bi-search"></i></span><input class="form-control border-start-0" name="search" placeholder="Cari nama, tag, atau serial number..." value="{{ request('search') }}"></div></div><div class="col-sm-4 col-lg-3"><select class="form-select" name="category"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category') == $category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-sm-4 col-lg-2"><select class="form-select" name="status"><option value="">Semua status</option>@foreach(['available' => 'Tersedia', 'assigned' => 'Assigned', 'maintenance' => 'Maintenance', 'retired' => 'Retired'] as $key => $label)<option value="{{ $key }}" @selected(request('status') === $key)>{{ $label }}</option>@endforeach</select></div><div class="col-sm-4 col-lg-2 d-grid"><button class="btn btn-dark"><i class="bi bi-funnel me-2"></i>Filter</button></div></form></div>
<div class="panel"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Aset</th><th>Kategori</th><th>Lokasi</th><th>Status</th><th>Kondisi</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($assets as $asset)<tr><td><div class="d-flex align-items-center gap-2"><span class="asset-icon"><i class="bi bi-laptop"></i></span><div><a href="{{ route('assets.show', $asset) }}" class="fw-semibold text-dark text-decoration-none">{{ $asset->name }}</a><div class="small text-secondary">{{ $asset->asset_tag }} @if($asset->brand) · {{ $asset->brand }} @endif</div></div></div></td><td>{{ $asset->category->name }}</td><td>{{ $asset->location?->name ?? 'Belum diatur' }}</td><td><span class="badge rounded-pill {{ $asset->status === 'available' ? 'text-bg-success' : ($asset->status === 'maintenance' ? 'text-bg-warning' : ($asset->status === 'retired' ? 'text-bg-secondary' : 'text-bg-primary')) }}">{{ ucfirst($asset->status) }}</span></td><td>{{ ucfirst($asset->condition) }}</td><td class="text-end"><a class="btn btn-sm btn-light" href="{{ route('assets.show', $asset) }}" title="Detail"><i class="bi bi-eye"></i></a><a class="btn btn-sm btn-light" href="{{ route('assets.edit', $asset) }}" title="Edit"><i class="bi bi-pencil"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center py-5"><i class="bi bi-inbox fs-2 text-secondary"></i><div class="mt-2">Belum ada aset yang cocok.</div></td></tr>@endforelse</tbody></table></div>@if($assets->hasPages())<div class="p-3 border-top">{{ $assets->links() }}</div>@endif</div>@endsection
