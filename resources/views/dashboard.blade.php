@extends('layouts.app')
@section('content')<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div class="eyebrow">Overview</div><h1 class="page-title h2 mb-1">Selamat datang, {{ str(auth()->user()->name)->before(' ') }}.</h1><p class="text-secondary mb-0">Pantau kesehatan inventaris dan aktivitas terbaru.</p></div><a href="{{ route('assets.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-2"></i>Tambah aset</a></div>
<div class="row g-3 mb-4"><div class="col-sm-6 col-xl-3"><div class="panel stat-card p-3 h-100"><div class="text-secondary small">Total aset</div><div class="value mt-2">{{ number_format($totalAssets) }}</div><div class="text-secondary small"><i class="bi bi-boxes me-1"></i>Terdaftar di sistem</div></div></div><div class="col-sm-6 col-xl-3"><div class="panel stat-card p-3 h-100"><div class="text-secondary small">Tersedia</div><div class="value mt-2">{{ number_format($availableAssets) }}</div><div class="text-success small"><i class="bi bi-check-circle me-1"></i>Siap digunakan</div></div></div><div class="col-sm-6 col-xl-3"><div class="panel stat-card p-3 h-100"><div class="text-secondary small">Dipinjamkan</div><div class="value mt-2">{{ number_format($assignedAssets) }}</div><div class="text-secondary small"><i class="bi bi-person-check me-1"></i>Assigned ke user</div></div></div><div class="col-sm-6 col-xl-3"><div class="panel stat-card p-3 h-100"><div class="text-secondary small">Perlu perhatian</div><div class="value mt-2">{{ number_format($maintenanceAssets) }}</div><div class="text-warning small"><i class="bi bi-wrench-adjustable me-1"></i>Dalam perbaikan</div></div></div></div>
<div class="row g-4"><div class="col-xl-8"><div class="panel"><div class="p-3 border-bottom d-flex justify-content-between align-items-center"><div><h2 class="h5 mb-1">Aset terbaru</h2><div class="small text-secondary">Update terakhir dari inventaris</div></div><a href="{{ route('assets.index') }}" class="small fw-semibold text-decoration-none">Lihat semua <i class="bi bi-arrow-right"></i></a></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Aset</th><th>Kategori</th><th>Status</th><th>Pemegang</th></tr></thead><tbody>@forelse($recentAssets as $asset)<tr><td><a href="{{ route('assets.show', $asset) }}" class="text-decoration-none fw-semibold text-dark">{{ $asset->name }}</a><div class="small text-secondary">{{ $asset->asset_tag }}</div></td><td>{{ $asset->category->name }}</td><td><span class="badge rounded-pill {{ $asset->status === 'available' ? 'text-bg-success' : ($asset->status === 'maintenance' ? 'text-bg-warning' : ($asset->status === 'retired' ? 'text-bg-secondary' : 'text-bg-primary')) }}">{{ ucfirst($asset->status) }}</span></td><td>{{ $asset->assignee?->name ?? 'Belum ditugaskan' }}</td></tr>@empty<tr><td colspan="4" class="text-center text-secondary py-5">Belum ada aset.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-xl-4"><div class="panel p-3 h-100"><div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="h5 mb-1">Komposisi aset</h2><div class="small text-secondary">{{ $locationsCount }} lokasi terdaftar</div></div><i class="bi bi-pie-chart text-success fs-4"></i></div>@foreach($categories as $category)<div class="d-flex align-items-center justify-content-between py-2 border-bottom"><span>{{ $category->name }}</span><span class="badge badge-soft rounded-pill">{{ $category->assets_count }}</span></div>@endforeach</div></div></div>@endsection
