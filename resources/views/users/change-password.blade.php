@extends('layouts.app')
@section('content')<div class="mb-4"><div class="eyebrow">Account security</div><h1 class="page-title h2 mb-1">Ganti password</h1><p class="text-secondary mb-0">Perbarui password akun IT Anda sendiri.</p></div><div class="panel p-3 p-lg-4" style="max-width: 680px"><form method="POST" action="{{ route('password.update') }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label fw-semibold">Password saat ini *</label><input class="form-control @error('current_password') is-invalid @enderror" type="password" name="current_password" required>@error('current_password')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="row g-3"><div class="col-md-6"><label class="form-label fw-semibold">Password baru *</label><input class="form-control @error('password') is-invalid @enderror" type="password" name="password" required>@error('password')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label fw-semibold">Konfirmasi password *</label><input class="form-control" type="password" name="password_confirmation" required></div></div><div class="d-flex justify-content-end gap-2 mt-4 pt-3 border-top"><a href="{{ route('dashboard') }}" class="btn btn-light">Batal</a><button class="btn btn-primary"><i class="bi bi-key me-2"></i>Simpan password</button></div></form></div>@endsection
