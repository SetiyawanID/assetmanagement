@extends('layouts.app')
@section('content')<div class="mb-4"><a href="{{ route('users.index') }}" class="text-secondary text-decoration-none small"><i class="bi bi-arrow-left me-1"></i>Kembali ke kelola akun</a><div class="eyebrow mt-3">Account profile</div><h1 class="page-title h2 mb-1">Edit Akun</h1><p class="text-secondary mb-0">Perbarui informasi {{ $user->name }}.</p></div><div class="row g-4"><div class="col-xl-8"><div class="panel p-3 p-lg-4"><form method="POST" action="{{ route('users.update', $user) }}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-6"><label class="form-label fw-semibold">Nama lengkap *</label><input class="form-control @error('name') is-invalid @enderror" name="name" value="{{ old('name', $user->name) }}" required>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label fw-semibold">Email *</label><input class="form-control @error('email') is-invalid @enderror" type="email" name="email" value="{{ old('email', $user->email) }}" required>@error('email')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label fw-semibold">Divisi</label><select class="form-select" name="division_id"><option value="">Belum diatur</option>@foreach($divisions as $division)<option value="{{ $division->id }}" @selected(old('division_id', $user->division_id) == $division->id)>{{ $division->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label fw-semibold">Role</label><input class="form-control" value="{{ $user->isSuperAdmin() ? 'Super Admin' : ($user->isAdmin() ? 'Admin' : 'User') }}" disabled></div></div><div class="d-flex justify-content-end gap-2 mt-4 pt-3 border-top"><a href="{{ route('users.index') }}" class="btn btn-light">Batal</a><button class="btn btn-primary"><i class="bi bi-check-lg me-2"></i>Simpan perubahan</button></div></form></div></div>@if($user->isAdmin() && auth()->user()->isSuperAdmin() && $user->id !== auth()->id())<div class="col-xl-4"><div class="panel p-3 p-lg-4 border-danger-subtle"><div class="asset-icon mb-3"><i class="bi bi-shield-lock"></i></div><h2 class="h5">Reset password</h2><p class="text-secondary small">Force reset hanya tersedia untuk akun Admin dan hanya dapat dilakukan Super Admin.</p><form method="POST" action="{{ route('users.password.update', $user) }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label fw-semibold">Password baru *</label><input class="form-control" type="password" name="password" minlength="8" required></div><div class="mb-3"><label class="form-label fw-semibold">Konfirmasi password *</label><input class="form-control" type="password" name="password_confirmation" minlength="8" required></div><button class="btn btn-outline-danger w-100"><i class="bi bi-shield-lock me-2"></i>Force reset</button></form></div></div>@endif</div>@endsection
