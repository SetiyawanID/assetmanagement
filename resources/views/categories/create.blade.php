@extends('layouts.app')
@section('content')
<div class="mb-4"><a href="{{ route('categories.index') }}" class="text-secondary text-decoration-none small"><i class="bi bi-arrow-left me-1"></i>Kembali ke kategori</a><div class="eyebrow mt-3">Asset setup</div><h1 class="page-title h2 mb-1">Tambah kategori</h1><p class="text-secondary mb-0">Kategori baru dari Admin akan menunggu approval Super Admin.</p></div>
<div class="panel p-3 p-lg-4" style="max-width: 680px"><form method="POST" action="{{ route('categories.store') }}">@csrf<div class="mb-3"><label class="form-label fw-semibold">Nama kategori *</label><input class="form-control @error('name') is-invalid @enderror" name="name" value="{{ old('name') }}" required>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="mb-3"><label class="form-label fw-semibold">Icon Bootstrap *</label><div class="input-group"><span class="input-group-text"><i id="category-icon-preview" class="bi {{ old('icon', 'bi-folder') }}"></i></span><input class="form-control @error('icon') is-invalid @enderror" name="icon" id="category-icon" value="{{ old('icon', 'bi-folder') }}" placeholder="bi-laptop" required></div><div class="form-text">Contoh: bi-laptop, bi-hdd, bi-router.</div>@error('icon')<div class="text-danger small mt-1">{{ $message }}</div>@enderror</div><div class="mb-3"><label class="form-label fw-semibold">Deskripsi</label><textarea class="form-control" name="description" rows="3">{{ old('description') }}</textarea></div><div class="text-end pt-3 border-top"><button class="btn btn-primary"><i class="bi bi-check-lg me-2"></i>Simpan kategori</button></div></form></div>
<script>document.getElementById('category-icon')?.addEventListener('input', event => { document.getElementById('category-icon-preview').className = 'bi '+event.target.value; });</script>
@endsection
