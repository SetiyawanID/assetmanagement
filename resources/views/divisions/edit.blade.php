@extends('layouts.app')
@section('content')<div class="mb-4"><a href="{{ route('divisions.index') }}" class="text-secondary text-decoration-none small"><i class="bi bi-arrow-left me-1"></i>Kembali ke list divisi</a><div class="eyebrow mt-3">Organization</div><h1 class="page-title h2 mb-1">Edit divisi</h1></div><div class="panel p-3 p-lg-4" style="max-width: 680px"><form method="POST" action="{{ route('divisions.update', $division) }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label fw-semibold">Nama divisi *</label><input class="form-control @error('name') is-invalid @enderror" name="name" value="{{ old('name', $division->name) }}" required>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="mb-3"><label class="form-label fw-semibold">Deskripsi</label><textarea class="form-control" name="description" rows="3">{{ old('description', $division->description) }}</textarea></div><div class="text-end pt-3 border-top"><button class="btn btn-primary"><i class="bi bi-check-lg me-2"></i>Simpan perubahan</button></div></form></div>@endsection
